export type ApiParams = {
  foodId: string;
  weight: number;
};

export type MutationParams = {
  foodId: string;
  weight: number;
  date: string;
};

export type SubmitParams = {
  foodId: string;
  date: string;
};
